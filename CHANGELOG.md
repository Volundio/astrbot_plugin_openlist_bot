# 🔄 更新日志

### v1.0.4

* ⚡ **优化**:
  * 群文件 URL 流式中转达到 `backup_retry_attempts` 仍失败后，会改用本地临时文件备用上传：先完整下载并校验大小，再上传到 OpenList，并在成功或失败后清理临时文件，降低 QQ CDN 大文件断流导致的备份失败率。
* 🐛 **修复**:
  * 修复 QQ 群文件同目录同名同大小但 `file_id` 不同时被误判为不同文件，导致重复改名备份的问题；同目录同名同大小文件会按重复项跳过，同目录同名不同大小文件才会自动改名备份，不会跨目录检查重复文件。

### v1.0.3

* ⚡ **优化**:
  * `ol autobackup enable` 触发的首次全量备份支持通过 `ol autobackup cancel [@群号]` 中途取消。
  * 群文件备份增加同一目标文件串行锁，避免同名文件并发写入同一路径。
  * 备份重试前会重新检查目标文件是否已存在，避免上一次上传实际成功后继续重复上传。
* 🐛 **修复**:
  * 修复群文件全量备份中同一目标文件可能被重复调度并发上传的问题。
  * 修复 QQ 群文件存在同名不同大小文件时可能只备份其中一个的问题；同名不同大小文件会自动改名备份。

### v1.0.2

* ✨ **功能**:
  * `ol upload` 改为最近附件上传：先发送图片、视频或文件，再在 5 分钟内执行 `ol upload [路径]` 上传该附件。
  * 上传支持从 OneBot 消息中解析图片、视频和文件段；群文件无 URL 时会尝试调用群文件接口获取下载地址。
* ⚡ **优化**:
  * 删除旧上传模式、上传等待状态和 `upload_mode_timeout` 配置项。
  * 删除引用消息上传路径，不再依赖 OneBot `reply` 段或 `get_msg` 接口。
  * 最近附件缓存只保存元数据，增加 5 分钟过期、500 条容量上限和机器人自消息过滤。
  * 浏览列表、分页、序号下载/预览/删除和相对路径上传按会话隔离，避免同一用户在不同群聊或私聊中串用导航状态。
  * 优化 `ol` 根指令、缺少参数、未知参数、权限不足和配置错误等提示文案，改为显示整理后的帮助、简短用法、示例和必要提示。
* 🐛 **修复**:
  * 修复指定 `@其他群号` 备份、恢复或配置自动备份时缺少目标群权限校验的问题；跨群操作要求发送者是目标群群主或管理员。
  * 修复 `ol restore /` 恢复 OpenList 根目录时路径可能被归一化为空的问题。
  * 修复群文件备份过滤时 `file_size` 为字符串可能导致大小比较异常的问题。
  * 修复备份重试缺少 `relative_path` 时可能无法计算目标目录的问题。

### v1.0.0

* ✨ **功能**:
  * 参考 `Foolllll-J/astrbot_plugin_openlistfile` 进行重构二次开发，保留原有 OpenList 文件管理能力。
  * 支持 `ol ls` 文件浏览，支持序号导航、分页、返回上级目录和中文别名。
  * 支持 `ol search` 文件搜索和 `ol info` 文件信息查看。
  * 支持 `ol download` 直接下载文件，并通过 AstrBot `File` 组件发送。
  * 支持获取下载链接并以 txt 附件发送，避免长链接被平台转成图片。
  * 支持 `ol upload` 上传模式，可直接发送文件或图片上传到指定目录。
  * 支持 `ol preview` 文本文件预览和压缩包内容查看。
  * 支持 `ol rm` 删除文件或目录，支持 `ol mkdir` 创建目录。
  * 支持 `ol backup` 群文件递归备份，可指定目标目录和群号。
  * 支持 `ol backup retry` 只重试上次备份失败项。
  * 支持 `ol autobackup` 群文件自动备份，开启后会立即执行一次全量备份。
  * 支持 `ol restore` 从 OpenList 恢复文件到群聊或私聊。
  * 支持全局共享配置和用户独立配置两种模式。
* ⚡ **优化**:
  * 重构项目目录结构，将上传、下载、浏览、备份、恢复、预览、配置等业务拆分到 `services/`。
  * 保留 AstrBot 插件入口和命令注册在 `main.py`，减少入口文件冗余代码。
  * 集中管理配置默认值、配置校验规则和 WebUI 字段映射。
  * 优化 WebUI 配置项显示，将 OpenList API 地址、对外下载地址、路径前缀修正的用途说明区分清楚。
  * 优化 OpenList 客户端请求逻辑，减少重复的鉴权、状态码和返回码处理。
  * 上传、下载、预览、恢复流程支持连接超时、读取超时、重试次数和诊断日志配置。
  * 群文件备份支持跳过目标目录中同名且大小一致的文件。
  * 自动备份使用后台任务和并发限制，避免阻塞消息处理。
  * 临时文件名增加随机性，降低高并发下的文件名冲突风险。
* 🐛 **修复**:
  * 修复下载直链以长文本发送时可能被平台转换成图片的问题。
  * 修复拼接 `/d/路径?sign=...` 导致签名失效的问题，直接下载改为优先调用 OpenList 真实下载链接接口。
  * 修复 `fixed_base_directory` 场景下真实下载路径处理不一致的问题。
  * 修复缓存配置未实际生效、按用户清理缓存不可靠的问题。
  * 修复 `allowed_extensions` 配置未参与业务判断的问题。
  * 修复下载进度消息过多刷屏的问题，详细进度改为诊断日志控制。
  * 修复 `ol autobackup` 不带参数时报必要参数缺失的问题。
  * 修复自动备份权限判断在部分适配器下无法识别群主/管理员的问题。
  * 修复下载、预览、恢复流程在异常时可能残留半截临时文件的问题。
  * 修复恢复文件时临时路径可能沿用上一轮循环值的问题。
